# 网站 Handbook 生成实验手册

## 当前结论

三个站点的 workflow 配置全部完成，所有生成器核心漏洞修复完毕。下一步是在服务器升级 Node.js 后同步代码，然后爬取 shopping_admin（需先手动登录）和 reddit。

### 当前状态

**已完成站点**
- WebArena shopping：`http://localhost:7770`（21 页、3335 元素、locator 验证 94.2%，未登录态）

**待爬取站点**
- WebArena shopping_admin：`http://localhost:7780`（workflow config 就绪，9 个工作流，184/184，0 冲突）
- WebArena reddit：`http://localhost:9999`（workflow config 就绪，9 个工作流，129/129，0 冲突）

**环境端口**
- shopping 控制台：`http://localhost:17771`
- reddit 控制台：`http://localhost:9998`（`http://localhost:19998` 本机映射）
- CDP Chrome：`http://127.0.0.1:9223`（9222 被系统 Chrome 占用）

### 生成器修复记录（Batch A + B + C）

| 改动 | 问题编号 | 文件 |
|---|---|---|
| `parseArgs` 支持 boolean flag，`--click-safe` 实现 unsafe href 过滤 | 问题 2 | `verify-handbook.mjs` |
| `classifyTask` 改为最长匹配置信度，消除顺序依赖 | 问题 5 | `task-workflows.mjs` |
| `category-navigation` / `catalog-aggregation` / `product-selection` evidence 正则区分 | 问题 4 | `task-workflows.mjs` |
| `buildWorkflowHandbook` + `classifyTask` 接受可注入 `workflowDefs` 参数 | 问题 1 | `task-workflows.mjs` |
| `crawl-site.mjs` + `rebuild-from-snapshots.mjs` 支持 `--workflow-config FILE` | 问题 1 | 两个文件 |
| `loadContextModel` 路径不存在时静默返回 null | P2 审查 | `context-model.mjs` |
| `waitForDomSettled` 去除硬编码 `li.product-item`，改用通用交互元素计数 | 问题 7 | `crawl-site.mjs` |
| shopping_admin workflow 配置（9 个工作流，184/184，0 冲突） | 问题 1 | `scripts/workflows/shopping-admin.mjs`（新文件）|
| reddit workflow 配置（9 个工作流，129/129，0 冲突） | 问题 1 | `scripts/workflows/reddit.mjs`（新文件）|

**仍未修复**
- 问题 6：验证只覆盖未登录态，需添加 `--cookie-file` 后跑登录态验证
- 问题 3：`execution-contract.json` 无 Runtime 强制（架构债，暂缓）

## 产物位置

- 生成器 skill：`generate-web-handbook/`
- shopping 站点 skill：`handbooks/webarena-shopping/`
- shopping_admin 任务语料：`handbooks/shopping_admin_corpus/task-corpus.json`（184 条）
- shopping_admin workflow 配置：`generate-web-handbook/scripts/workflows/shopping-admin.mjs`
- reddit workflow 配置：`generate-web-handbook/scripts/workflows/reddit.mjs`
- 恢复检查点：`handbooks/webarena-shopping/manifest.json`
- selector 全量快照：`handbooks/webarena-shopping/snapshots/selectors.json`
- 中文简版手册：`handbooks/webarena-shopping/references/handbook.md`
- 原图与标注截图：`handbooks/webarena-shopping/screenshots/`
- locator 实测结果：`handbooks/webarena-shopping/verification.json`

## 运行环境

### 架构决策

**爬取和验证在服务器运行，代码开发在本机。**

原因：
- 本机通过 CDP over SSH 隧道控制浏览器，每次 `page.evaluate()` 都走隧道 round-trip，延迟高且不稳定
- 服务器本地 CDP 零延迟，爬取速度快 5-10 倍
- tmux 里跑，SSH 断开后爬取继续

**Node 版本要求：** 服务器默认 v12，项目使用 ESM + `Array.at()` + `structuredClone`，需要 v18+。

### 1. 服务器环境准备（首次）

```bash
ssh root@218.245.63.97 -p 2286

# 安装 nvm 和 Node v22
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
source ~/.bashrc
nvm install 22
nvm use 22
node --version  # 应输出 v22.x.x

# 安装依赖
cd /data/handbook_web/generate-web-handbook
npm install --no-audit --no-fund
```

### 2. 同步代码到服务器

```bash
rsync -av --exclude node_modules --exclude .git \
  /Users/rebekah/handbook_web/ \
  root@218.245.63.97:/data/handbook_web/ \
  -e "ssh -p 2286"
```

### 3. 在服务器启动 CDP Chrome

```bash
ssh root@218.245.63.97 -p 2286
tmux new -s chrome

browser_profile_dir=$(mktemp -d /tmp/handbook-chrome.XXXXXX)
google-chrome \
  --headless=new \
  --remote-debugging-address=127.0.0.1 \
  --remote-debugging-port=9222 \
  --user-data-dir="$browser_profile_dir" \
  --window-size=1440,1000 \
  --no-first-run \
  --no-default-browser-check \
  --disable-background-networking \
  about:blank

# 检查 CDP
curl http://127.0.0.1:9222/json/version
```

### 4. 建立 SSH 隧道（本机查看页面用）

```bash
ssh -N \
  -o ExitOnForwardFailure=yes \
  -o ServerAliveInterval=30 \
  -o ServerAliveCountMax=3 \
  -L 7770:localhost:7770 \
  -L 17771:localhost:7771 \
  -L 9999:localhost:9999 \
  -L 19998:localhost:9998 \
  root@218.245.63.97 -p 2286
```

检查服务（本机）：

```bash
curl -I http://127.0.0.1:7770/    # shopping
curl http://127.0.0.1:17771/status
curl -I http://127.0.0.1:9999/    # reddit
curl http://127.0.0.1:19998/status
```

## 抓取、恢复与验证

以下命令在**服务器**上运行（`/data/handbook_web` 目录）。

### shopping（已完成，如需重跑）

```bash
node generate-web-handbook/scripts/crawl-site.mjs \
  --url http://localhost:7770 \
  --site webarena-shopping \
  --output handbooks \
  --cdp http://127.0.0.1:9222 \
  --max-pages 30 \
  --max-depth 2 \
  --fresh
```

### shopping_admin（待爬取）

Magento Admin 首页会重定向到登录页，爬取前必须先在 CDP Chrome 里手动登录。

```bash
# 步骤 1：用 Chrome DevTools 或非 headless Chrome 打开 http://localhost:7780
# 用 admin 账号登录，确认 session cookie 写入 profile 目录

# 步骤 2：爬取
node generate-web-handbook/scripts/crawl-site.mjs \
  --url http://localhost:7780 \
  --site webarena-shopping-admin \
  --output handbooks \
  --cdp http://127.0.0.1:9222 \
  --max-pages 15 \
  --max-depth 2 \
  --coverage-corpus handbooks/shopping_admin_corpus/task-corpus.json \
  --site-key shopping_admin \
  --workflow-config generate-web-handbook/scripts/workflows/shopping-admin.mjs \
  --fresh
```

### reddit（待爬取，workflow config 待写）

```bash
node generate-web-handbook/scripts/crawl-site.mjs \
  --url http://localhost:9999 \
  --site webarena-reddit \
  --output handbooks \
  --cdp http://127.0.0.1:9222 \
  --max-pages 20 \
  --max-depth 2 \
  --workflow-config generate-web-handbook/scripts/workflows/reddit.mjs \
  --fresh
```

### 通用命令

中断后恢复：去掉 `--fresh` 重跑同一命令。

验证 locator（服务器）：

```bash
node generate-web-handbook/scripts/verify-handbook.mjs \
  --handbook handbooks/webarena-shopping \
  --cdp http://127.0.0.1:9222 \
  --max-actions 200 \
  --click-safe
```

从快照重建（不需要 CDP）：

```bash
node generate-web-handbook/scripts/rebuild-from-snapshots.mjs \
  --handbook handbooks/webarena-shopping
```

### 产物同步回本机

```bash
rsync -av \
  root@218.245.63.97:/data/handbook_web/handbooks/ \
  /Users/rebekah/handbook_web/handbooks/ \
  -e "ssh -p 2286"
```

校验 skill（本机）：

```bash
python3 /Users/rebekah/.codex/skills/.system/skill-creator/scripts/quick_validate.py \
  generate-web-handbook

python3 /Users/rebekah/.codex/skills/.system/skill-creator/scripts/quick_validate.py \
  handbooks/webarena-shopping
```

## 迭代记录

| 轮次 | 观察到的问题 | 生成器改进 | 验证结果 |
|---|---|---|---:|
| 1 | 重定向页面重复抓取；随机 ID；商品链接混入脚本文本 | 最终 URL 去重；过滤动态 ID；只读取可见文本 | 产物可生成 |
| 2 | 重复的商品按钮退化为 `nth-of-type` | 加入”商品卡片文本 + 角色 + 名称”上下文 locator | 上下文 locator 61 个 |
| 3 | DOM 推断名称与可访问性名称不一致 | 增加只读验证器；对装饰图标使用非精确名称匹配 | 202/327，61.77% |
| 4 | 链接名称仍受图标、动态数量和标题影响 | 无稳定语义时优先唯一 `href`；表单控件精确匹配 | 307/327，93.88% |
| 5 | 必填标记和视图切换名称仍不一致 | 稳定属性 selector 优先于猜测的可访问性名称 | 327/327，100% |
| P1 | 执行合约只有自然语言 assertions，无法被 Runtime 强制执行 | `readCurrentPageState_v1` 通用动作 + `verifyState` 可执行 JS 函数 + `--click-safe` 验证 | 500 抽样 471/500，94.2% |
| P2 | 对抗式审查发现 7 个问题：`parseArgs` 不一致、`--click-safe` 安全过滤不足、`contextModelPath` 路径不存在时崩溃、`queue.sort` 性能、`covered` 判断逻辑过严、订单类 workflow 缺少补充查询预算、测试未覆盖新功能 | 统一 `parseArgs` 布尔 flag 处理、添加 unsafe href 过滤、`context-model.mjs` 添加 try-catch、`queue.sort` 延迟排序、`covered` 增加 structures/locators 判断、`workflowBudgets` 扩展 `supplementalQueriesMax` 到订单类、测试添加 `readCurrentPageState_v1` 和 `verifyState` 断言 | 12/12 workflow 全部 covered=true，supplementalQueriesMax 正确分配 |
| Batch A+B | 第一性原理对抗式审查发现 7 个系统性问题：WORKFLOWS 硬编码 shopping、`--click-safe` 静默损坏、三个 catalog workflow evidence 完全重叠、classifyTask 顺序优先而非置信度、execution-contract 无 runtime 强制、未登录态验证覆盖不足、waitForDomSettled Magento 专有 | parseArgs boolean flag 修复、click-safe 实现、classifyTask 改最长匹配、evidence 正则区分、buildWorkflowHandbook workflowDefs 参数注入、crawl-site + rebuild 支持 --workflow-config、context-model crash 修复、shopping-admin.mjs workflow 配置（9 工作流） | test-generator 全通过；shopping-admin 静态路由 184/184，0 冲突；问题 6/7/3 待 CDP 环境验证 |
| Batch C | 问题 7：waitForDomSettled 硬编码 li.product-item（非 Magento 站点计数恒 0，稳定检测退化）；reddit workflow 配置缺失 | waitForDomSettled 改用通用交互元素（button/input/select/role=button/tab）计数；reddit.mjs 9 个工作流基于 129 条 WebArena 真实任务设计 | 问题 7 已修复（无 CDP 专项验证需求）；reddit 静态路由 129/129，0 冲突，0 fallback |

## 无副作用操作抽样

| 操作 | 结果 |
|---|---|
| 点击 `Beauty & Personal Care` 分类 | 成功到达 `/beauty-personal-care.html` |
| 点击 `Advanced Search` | 成功到达 `/catalogsearch/advanced/` |
| 填写登录邮箱但不提交 | 字段值与输入一致 |

未测试加入购物车、登录提交、注册、结账、删除等会改变服务器状态的动作。

## 收敛标准

本轮停止迭代，因为：

1. 抓取过程无错误，重复重定向已正确跳过。
2. 327 个主 locator 全部在实时页面中唯一且可见。
3. 三个常用、无副作用操作全部成功。
4. 最后一轮没有新增 selector 失败。

扩大覆盖范围时，把 `--max-pages` 和 `--max-depth` 逐步增加；每次扩容后重新
运行验证器，不要直接假设现有规则能覆盖全站。

